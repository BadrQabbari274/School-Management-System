@model StudentManagementSystem.ViewModels.CompetenciesViewModel

@{
    ViewData["Title"] = "إضافة جدارة جديدة";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/css/field-styles.css" rel="stylesheet" />
<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

<div class="container-fluid p-4">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
            <div class="card border-0 shadow-lg">
                <div class="card-header bg-primary text-white text-center py-4">
                    <h3 class="mb-0 fw-bold">
                        <i class="fas fa-plus-circle me-2"></i>إضافة جدارة جديدة
                    </h3>
                </div>
                <div class="card-body p-5">
                    <form asp-action="Create" method="post" id="createCompetencyForm">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-4"></div>

                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label asp-for="Name" class="form-label fw-bold text-primary">
                                    <i class="fas fa-medal me-2"></i>اسم الجدارة
                                </label>
                                <input asp-for="Name" class="form-control form-control-lg"
                                       placeholder="أدخل اسم الجدارة" id="nameInput" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                                <div id="nameError" class="text-danger mt-1" style="display:none;">
                                    <i class="fas fa-exclamation-circle me-1"></i>
                                    لا يمكن أن يبدأ اسم الجدارة بأرقام.
                                </div>
                                <small id="nameCharCounter" class="form-text text-muted"></small>
                            </div>

                            <div class="col-md-6 mb-4">
                                <label asp-for="Duration" class="form-label fw-bold text-primary">
                                    <i class="fas fa-clock me-2"></i>المدة
                                </label>
                                <input asp-for="Duration" class="form-control form-control-lg"
                                       placeholder="مثال: 3 أشهر، 6 أسابيع" />
                                <span asp-validation-for="Duration" class="text-danger"></span>
                                <small id="durationCharCounter" class="form-text text-muted"></small>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label asp-for="Section_Id" class="form-label fw-bold text-primary">
                                    <i class="fas fa-sitemap me-2"></i>القسم
                                </label>
                                <select asp-for="Section_Id" class="form-control form-control-lg" asp-items="Model.Sections">
                                    <option value="">-- اختر القسم --</option>
                                </select>
                                <span asp-validation-for="Section_Id" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-4">
                                <label asp-for="Max_Outcome" class="form-label fw-bold text-primary">
                                    <i class="fas fa-target me-2"></i>الحد الأقصى للنتائج
                                </label>
                                <input asp-for="Max_Outcome" type="number" min="1" class="form-control form-control-lg"
                                       placeholder="أدخل رقم أكبر من صفر" />
                                <span asp-validation-for="Max_Outcome" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-12 mb-4">
                                <div class="form-check form-switch">
                                    <input asp-for="IsActive" class="form-check-input" type="checkbox"
                                           id="isActiveSwitch" checked />
                                    <label class="form-check-label fw-bold text-primary" for="isActiveSwitch">
                                        <i class="fas fa-toggle-on me-2"></i>نشط
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div class="mb-4">
                            <div class="card bg-light border-0">
                                <div class="card-body p-3">
                                    <h6 class="card-title text-primary mb-3">
                                        <i class="fas fa-info-circle me-2"></i>معلومات إضافية
                                    </h6>
                                    <div class="row">
                                        <div class="col-md-6 mb-2">
                                            <small class="text-muted">
                                                <i class="fas fa-user me-1"></i>
                                                ستنشأ بواسطة: <strong>@ViewBag.CurrentUserName</strong>
                                            </small>
                                        </div>
                                        <div class="col-md-6">
                                            <small class="text-muted">
                                                <i class="fas fa-calendar me-1"></i>
                                                تاريخ الإنشاء: <strong>@DateTime.Now.ToString("dd/MM/yyyy")</strong>
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <button type="submit" class="btn btn-primary btn-lg w-100 mb-3" id="submitBtn">
                                    <i class="fas fa-save me-2"></i>حفظ الجدارة
                                </button>
                            </div>
                            <div class="col-md-6">
                                <a asp-action="Index" class="btn btn-outline-secondary btn-lg w-100">
                                    <i class="fas fa-arrow-left me-2"></i>العودة للقائمة
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 20px;
        overflow: hidden;
    }

    .card-header {
        background: linear-gradient(135deg, #fd7e14 0%, #e8590c 100%);
        border: none;
    }

    .form-control {
        border-radius: 10px;
        border: 2px solid #e9ecef;
        padding: 12px 15px;
        transition: all 0.3s ease;
    }

        .form-control:focus {
            border-color: #fd7e14;
            box-shadow: 0 0 0 0.2rem rgba(253, 126, 20, 0.25);
        }

    .btn-primary {
        background: linear-gradient(135deg, #fd7e14 0%, #e8590c 100%);
        border: none;
        border-radius: 10px;
        padding: 12px 24px;
        transition: all 0.3s ease;
    }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(253, 126, 20, 0.4);
        }

    .btn-outline-secondary {
        border-radius: 10px;
        padding: 12px 24px;
        transition: all 0.3s ease;
        border: 2px solid #6c757d;
    }

        .btn-outline-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(108, 117, 125, 0.3);
        }

    .form-check-input:checked {
        background-color: #fd7e14;
        border-color: #fd7e14;
    }

    .text-primary {
        color: #fd7e14 !important;
    }

    .form-label {
        margin-bottom: 8px;
        font-size: 1.1rem;
    }

    .bg-light {
        background-color: #f8f9fa !important;
        border-radius: 10px;
    }

    .is-invalid {
        border-color: #dc3545 !important;
    }
</style>

<!-- Success/Error Messages -->
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle me-2"></i>
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="fas fa-exclamation-circle me-2"></i>
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        $(document).ready(function () {
            const nameInput = $('#nameInput');
            const nameError = $('#nameError');
            const submitBtn = $('#submitBtn');

            // Function to validate that the name doesn't start with numbers
            function validateName(value) {
                const trimmedValue = value.trim();
                return trimmedValue !== '' && !/^\d/.test(trimmedValue);
            }

            // Function to update the validation state
            function updateNameValidation() {
                const isValid = validateName(nameInput.val());
                if (isValid) {
                    nameError.hide();
                    nameInput.removeClass('is-invalid');
                } else {
                    nameError.show();
                    nameInput.addClass('is-invalid');
                }
            }

            // Real-time validation
            nameInput.on('input', function() {
                updateNameValidation();

                // Character counter for name
                const maxLength = 200;
                const currentLength = $(this).val().length;
                const remaining = maxLength - currentLength;

                $('#nameCharCounter').text('عدد الأحرف المتبقية: ' + remaining);

                if (remaining < 20) {
                    $('#nameCharCounter').removeClass('text-muted').addClass('text-warning');
                } else {
                    $('#nameCharCounter').removeClass('text-warning').addClass('text-muted');
                }
            });

            // Character counter for duration
            $('#Duration').on('input', function() {
                const maxLength = 100;
                const currentLength = $(this).val().length;
                const remaining = maxLength - currentLength;

                $('#durationCharCounter').text('عدد الأحرف المتبقية: ' + remaining);

                if (remaining < 10) {
                    $('#durationCharCounter').removeClass('text-muted').addClass('text-warning');
                } else {
                    $('#durationCharCounter').removeClass('text-warning').addClass('text-muted');
                }
            });

            // Paste validation
            nameInput.on('paste', function(e) {
                setTimeout(updateNameValidation, 0);
            });

            // Form submission validation
            $('#createCompetencyForm').on('submit', function (e) {
                let isFormValid = true;

                // Validate name
                if (!validateName(nameInput.val())) {
                    isFormValid = false;
                    nameInput.addClass('is-invalid');
                    nameError.show();
                }

                // Validate required fields
                const requiredFields = ['#nameInput', '#Duration', '#Section_Id', '#Max_Outcome'];
                requiredFields.forEach(function(field) {
                    const $field = $(field);
                    if (!$field.val() || $field.val().trim() === '') {
                        isFormValid = false;
                        $field.addClass('is-invalid');
                    } else {
                        $field.removeClass('is-invalid');
                    }
                });

                // Validate Max_Outcome is greater than 0
                const maxOutcome = parseInt($('#Max_Outcome').val());
                if (!maxOutcome || maxOutcome <= 0) {
                    isFormValid = false;
                    $('#Max_Outcome').addClass('is-invalid');
                }

                if (!isFormValid) {
                    e.preventDefault();
                    alert('يرجى التأكد من ملء جميع الحقول المطلوبة بشكل صحيح.');
                    nameInput.focus();
                    return false;
                }

                // Show loading state
                submitBtn.prop('disabled', true);
                submitBtn.html('<i class="fas fa-spinner fa-spin me-2"></i>جاري الحفظ...');
            });

            // Remove validation classes on input change
            $('input, select').on('change keyup', function () {
                $(this).removeClass('is-invalid');
            });

            // Auto-focus on first input
            $('#Section_Id').focus();

            // Initialize character counters
            nameInput.trigger('input');
            $('#Duration').trigger('input');

            // AJAX name uniqueness check (optional)
            let nameTimeout;
            nameInput.on('input', function() {
                const name = $(this).val();
                if (name.length > 2) {
                    clearTimeout(nameTimeout);
                    nameTimeout = setTimeout(function() {
                        $.post('@Url.Action("IsNameUnique")', { name: name }, function(data) {
                            if (!data) {
                                nameInput.addClass('is-invalid');
                                // You can show a specific error message here
                            }
                        });
                    }, 500);
                }
            });
        });
    </script>
}