@*  <div class="col-md-9 p-4">
            <div class="card shadow">
                <div class="card-header bg-light">
                    <h2 class="mb-0">طلاب</h2>
                </div>
                
                <div class="card-body">
                    @using (Html.BeginForm("Save", "Attendance", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate" }))
                    {
                        @Html.AntiForgeryToken()
                        
                        <!-- معلومات التاريخ والفصل -->
                        <div class="row mb-4">
                            <div class="col-md-6">
                                <label class="form-label">التاريخ:</label>
                                @Html.TextBoxFor(m => m.Date, "{0:yyyy-MM-dd}", new { @class = "form-control", @type = "date" })
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">الفصل:</label>
                                @Html.TextBoxFor(m => m.ClassName, new { @class = "form-control" })
                            </div>
                        </div>

                        <!-- جدول الحضور -->
                        <div class="table-responsive">
                            <table class="table table-bordered table-striped">
                                <thead class="table-dark">
                                    <tr>
                                        <th style="width: 30%;">الطالب</th>
                                        <th style="width: 15%;">غائب</th>
                                        <th style="width: 20%;">حاضر بدون حافز</th>
                                        <th style="width: 35%;">السبب</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @for (int i = 0; i < Model.Students.Count; i++)
                                    {
                                        <tr>
                                            <td>
                                                @Html.HiddenFor(m => m.Students[i].StudentId)
                                                @Html.DisplayFor(m => m.Students[i].StudentName)
                                                @Html.HiddenFor(m => m.Students[i].StudentName)
                                            </td>
                                            <td class="text-center">
                                                @Html.CheckBoxFor(m => m.Students[i].IsAbsent, new { @class = "form-check-input", @style = "transform: scale(1.5);" })
                                            </td>
                                            <td class="text-center">
                                                @Html.CheckBoxFor(m => m.Students[i].IsPresentWithoutIncentive, new { @class = "form-check-input", @style = "transform: scale(1.5);" })
                                            </td>
                                            <td>
                                                <select name="Students[@i].Reason" class="form-select bg-warning text-dark">
                                                    <option value="">اختر السبب</option>
                                                    <option value="مرض">مرض</option>
                                                    <option value="ظروف عائلية">ظروف عائلية</option>
                                                    <option value="أخرى">أخرى</option>
                                                </select>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>

                        <!-- أزرار الحفظ -->
                        <div class="row mt-4">
                            <div class="col-12 text-center">
                                <button type="submit" class="btn btn-success btn-lg me-3">
                                    <i class="fas fa-save"></i> حفظ الحضور
                                </button>
                                <button type="reset" class="btn btn-secondary btn-lg">
                                    <i class="fas fa-undo"></i> إعادة تعيين
                                </button>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<!-- رسائل التنبيه -->
@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show position-fixed top-0 end-0 m-3" role="alert" style="z-index: 1050;">
        <i class="fas fa-check-circle"></i> @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show position-fixed top-0 end-0 m-3" role="alert" style="z-index: 1050;">
        <i class="fas fa-exclamation-circle"></i> @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@section Scripts {
    <script>
        $(document).ready(function() {
            // تنسيق الشيك بوكس
            $('.form-check-input').change(function() {
                if ($(this).is(':checked')) {
                    $(this).closest('tr').addClass('table-warning');
                } else {
                    $(this).closest('tr').removeClass('table-warning');
                }
            });

            // إخفاء رسائل التنبيه تلقائياً
            setTimeout(function() {
                $('.alert').fadeOut();
            }, 3000);

            // تفعيل Bootstrap tooltips
            $('[data-bs-toggle="tooltip"]').tooltip();
        });
    </script>
}

<style>
    .table th {
        background-color: #495057;
        color: white;
        text-align: center;
        vertical-align: middle;
    }
    
    .table td {
        vertical-align: middle;
        text-align: center;
    }
    
    .table td:first-child {
        text-align: right;
        font-weight: bold;
    }
    
    .bg-warning {
        background-color: #f39c12 !important;
    }
    
    .form-select {
        border-radius: 8px;
    }
    
    .card {
        border-radius: 15px;
    }
    
    .btn {
        border-radius: 8px;
    }
    
    .form-check-input:checked {
        background-color: #f39c12;
        border-color: #f39c12;
    }
</style> *@