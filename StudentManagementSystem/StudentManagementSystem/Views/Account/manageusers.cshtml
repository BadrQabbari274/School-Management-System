@model List<StudentManagementSystem.ViewModels.UserManagementViewModel>

@{
    ViewData["Title"] = "≈œ«—… «·„” Œœ„Ì‰";
}

<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <style>
        /* General Body Styles */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            direction: rtl;
            margin: 0;
            padding: 0;
        }

        /* Header Section */
        .header-section {
            background: linear-gradient(135deg, #fff, #fff); /* Warm gradient */
            color: #ff6b35;
            text-align: center;
            padding: 25px 0;
            margin-bottom: 30px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15); /* Stronger shadow */
            border-radius: 0 0 20px 20px; /* Rounded bottom corners */
        }

        .header-title {
            font-size: 32px; /* Larger title */
            font-weight: bold;
            margin: 0;
            letter-spacing: 1px;
        }

        /* Container for content */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Control Buttons (Add New) */
        .controls {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px; /* More space */
            gap: 20px; /* Add gap between elements */
        }

        /* --- Begin: Search Bar Styles from Class.Index.cshtml --- */
        .search-section {
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 3px 12px rgba(0, 0, 0, 0.1);
            margin-bottom: 25px;
            display: block;
            max-width: 100%;
        }

        .search-container {
            position: relative;
            max-width: 500px;
            margin: 0 auto;
        }

        .search-input {
            width: 100%;
            padding: 12px 50px 12px 15px;
            border: 2px solid #e0e6ed;
            border-radius: 25px;
            font-size: 16px;
            transition: all 0.3s ease;
            background-color: #f8f9fa;
        }

        .search-input:focus {
            outline: none;
            border-color: #007bff;
            background-color: white;
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
        }

        .search-icon {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            font-size: 18px;
        }

        .clear-search {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: #6c757d;
            font-size: 18px;
            cursor: pointer;
            display: none;
            padding: 0;
            width: 24px;
            height: 24px;
        }

        .clear-search:hover {
            color: #dc3545;
        }

        .search-results-info {
            text-align: center;
            margin-top: 15px;
            color: #6c757d;
            font-style: italic;
        }

        .no-results {
            text-align: center;
            padding: 40px;
            color: #dc3545;
            font-style: italic;
            font-size: 18px;
        }

        /* Highlight search results */
        .highlight {
            background-color: #fff3cd;
            font-weight: bold;
            padding: 2px 4px;
            border-radius: 3px;
        }

        /* Hide rows that don't match search */
        .hidden-row {
            display: none !important;
        }
        /* --- End: Search Bar Styles from Class.Index.cshtml --- */


        /* Search Section Styling */
        .search-section {
            display: flex;
            align-items: center;
            gap: 15px; /* Space between search input and button */
        }

        .input-group {
            max-width: 400px;
            margin-bottom: 0 !important;
        }

        /* Search Input Styling */
        .form-control {
            border-radius: 12px !important; /* Smooth edges for search input */
            border: 1px solid #ced4da;
            padding: 10px 15px;
            font-size: 14px;
        }

            .form-control:focus {
                box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
                border-color: #007bff;
            }

        /* Search Button Styling */
        .btn-search {
            background: linear-gradient(135deg, #007bff, #0056b3);
            border: none;
            border-radius: 12px !important; /* Smooth edges */
            color: white;
            padding: 10px 20px;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 3px 8px rgba(0, 123, 255, 0.2);
        }

            .btn-search:hover {
                background: linear-gradient(135deg, #0056b3, #004085);
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(0, 123, 255, 0.3);
                color: white;
            }

        /* General Button Styles */
        .btn {
            padding: 10px 20px; /* More padding */
            border: none;
            color: #6c757d;
            font-size: 18px;
            color: #6c757d;
            font-size: 18px;
            color: #6c757d;
            font-size: 18px;
            cursor: pointer;
            font-size: 15px; /* Slightly larger font */
            font-weight: 600; /* Bolder text */
            text-decoration: none;
            display: inline-flex; /* Align icon and text */
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease; /* Smooth transitions */
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
            color: white; /* Default text color for gradient buttons */
        }

            .btn i {
                margin-left: 8px; /* Space between icon and text */
            }

        /* Specific Button Styles with Gradients and Hover Effects */
        .btn-add {
            background: linear-gradient(135deg, #28a745, #20c997); /* Green gradient */
        }

            .btn-add:hover {
                background: linear-gradient(135deg, #218838, #1e7e34); /* Darker green on hover */
                transform: translateY(-2px); /* Lift effect */
                box-shadow: 0 6px 12px rgba(40, 167, 69, 0.3); /* Stronger shadow on hover */
            }

        .btn-info {
            background: linear-gradient(135deg, #17a2b8, #138496); /* Blue gradient */
        }

            .btn-info:hover {
                background: linear-gradient(135deg, #117a8b, #0e6e7e);
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(23, 162, 184, 0.3);
            }

        .btn-warning {
            background: linear-gradient(135deg, #ffc107, #e0a800); /* Yellow gradient */
            color: #333; /* Darker text for contrast */
        }

            .btn-warning:hover {
                background: linear-gradient(135deg, #e0a800, #c69500);
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(255, 193, 7, 0.3);
            }

        .btn-danger {
            background: linear-gradient(135deg, #dc3545, #c82333); /* Red gradient */
        }

            .btn-danger:hover {
                background: linear-gradient(135deg, #c82333, #bd2130);
                transform: translateY(-2px);
                box-shadow: 0 6px 12px rgba(220, 53, 69, 0.3);
            }

        .btn-sm {
            padding: 6px 12px; /* Adjusted for small buttons */
            font-size: 13px;
            margin-left: 5px;
        }

        /* Alert Messages */
        .alert {
            padding: 15px;
            margin-bottom: 25px;
            border-radius: 8px;
            font-size: 16px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .alert-success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        /* Table Styles */
        .courses-table {
            width: 100%;
            background-color: white;
            border-radius: 12px; /* More rounded table */
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1); /* Stronger table shadow */
            overflow: hidden; /* Ensures rounded corners apply to content */
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin: 0;
        }

            .table th,
            .table td {
                padding: 18px; /* More padding in cells */
                text-align: center;
                border-bottom: 1px solid #eee;
            }

            .table th {
                background-color: #f0f2f5; /* Lighter header background */
                font-weight: bold;
                color: #555;
                border-bottom: 2px solid #dee2e6;
                font-size: 15px;
            }

            .table tbody tr:hover {
                background-color: #e9ecef; /* Lighter hover effect */
            }

        .btn-group {
            display: flex;
            gap: 8px; /* More space between action buttons */
            justify-content: center;
        }

        .no-data {
            text-align: center;
            padding: 40px;
            color: #888; /* Softer color */
            font-style: italic;
            font-size: 18px;
        }

        /* Status Badges */
        .status-badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 600;
            text-align: center;
        }

        .status-active {
            background-color: #28a745;
            color: #fff !important;
        }

        .status-inactive {
            background-color: #f8d7da;
            color: #dc3545;
        }

        /* Custom Modal Styles (for Delete Confirmation) */
        .custom-modal .modal-content {
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            border: none;
        }

        .custom-modal .modal-header {
            background: linear-gradient(135deg, #dc3545, #c82333);
            color: white;
            border-radius: 15px 15px 0 0;
            padding: 20px;
            border-bottom: none;
        }

        .custom-modal .modal-title {
            font-weight: bold;
            font-size: 24px;
        }

        .custom-modal .btn-close {
            filter: invert(1) brightness(2); /* Make close button white */
        }

        .custom-modal .modal-body {
            padding: 30px;
            font-size: 18px;
            color: #333;
            text-align: center;
        }

        .custom-modal .modal-footer {
            border-top: none;
            padding: 20px;
            justify-content: center;
            gap: 15px;
        }

        /* Custom Styles for Profile and Delete Buttons */
        .profile-btn {
            border-top-right-radius: 0.5rem !important;
            border-bottom-right-radius: 0.5rem !important;
            border-top-left-radius: 0 !important;
            border-bottom-left-radius: 0 !important;
        }

        .delete-btn {
            border-top-left-radius: 0.5rem !important;
            border-bottom-left-radius: 0.5rem !important;
            border-top-right-radius: 0 !important;
            border-bottom-right-radius: 0 !important;
        }

        /* Responsive Design */
        @@media (max-width: 768px) {
            .container

        {
            padding: 10px;
        }

        .header-title {
            font-size: 24px;
        }

        .controls {
            flex-direction: column;
            gap: 15px;
            align-items: stretch;
        }

        .search-section {
            justify-content: center;
        }

        .input-group {
            max-width: 100%;
        }

        .table th,
        .table td {
            padding: 10px;
            font-size: 12px;
        }

        .btn-group {
            flex-direction: column;
            gap: 5px;
        }

        .btn {
            padding: 8px 15px;
            font-size: 13px;
        }

        }
    </style>
</head>
<body>
    <div class="header-section">
        <div class="container">
            <h1 class="header-title">≈œ«—… «·„” Œœ„Ì‰</h1>
        </div>
    </div>

    <div class="container">

        <div class="controls">
            <!-- Left: Active Users Label -->
            <div style="font-weight: bold; color: #28a745;">
                <i class="fas fa-users"></i> «·„” Œœ„Ì‰ «·‰‘ÿÌ‰ ›ﬁÿ
            </div>

            <!-- Center: Search Section (Class.Index style) -->
            <div class="search-section">
                <div class="search-container">
                    <input type="text" id="searchInput" class="search-input" placeholder="«»ÕÀ ⁄‰ «·„” Œœ„Ì‰">
                    <i class="fas fa-search search-icon"></i>
                    <button type="button" class="clear-search" id="clearSearch">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
                <div class="search-results-info" id="searchResults"></div>
            </div>

            <!-- Right: Add User Button -->
            <div>
                <a href="@Url.Action("Register", "Account")" class="btn btn-add">
                    <i class="fas fa-plus-circle"></i> ≈÷«›… „” Œœ„ ÃœÌœ
                </a>
            </div>
        </div>

        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i>
                @TempData["Success"]
            </div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="alert alert-danger">
                <i class="fas fa-exclamation-circle"></i>
                @TempData["Error"]
            </div>
        }

        <div class="courses-table">
            <table class="table">
                <thead>
                    <tr>
                        <th>«·«”„</th>
                        <th>«”„ «·„” Œœ„</th>
                        <th>«·œÊ—</th>
                        <th> «—ÌŒ «·≈‰‘«¡</th>
                        <!-- <th>«·Õ«·…</th> -->
                        <th> „ «·≈‰‘«¡ »Ê«”ÿ…</th>
                        <th>¬Œ—  ⁄œÌ·</th>
                        <th>«·⁄„·Ì« </th>
                    </tr>
                </thead>
                <tbody id="usersTableBody">
                    @if (Model != null && Model.Any(u => u.IsActive))
                    {
                        @foreach (var user in Model.Where(u => u.IsActive))
                        {
                            <tr data-username="@user.Username.ToLower()" data-name="@user.Name.ToLower()" data-role="@user.RoleName.ToLower()">
                                <td>@user.Name</td>
                                <td>@user.Username</td>
                                <td>@user.RoleName</td>
                                <td>@user.Date.ToString("dd/MM/yyyy")</td>
                                <!--
                                <td>
                                    <span class="status-badge status-active">
                                        ‰‘ÿ
                                    </span>
                                </td>
                                -->
                                <td>@user.CreatedBy</td>
                                <td>@user.LastEditBy</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a href="@Url.Action("UserProfile", "Account", new { id = user.Id })"
                                           class="btn btn-info btn-sm profile-btn" title="⁄—÷ «·„·› «·‘Œ’Ì">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="@Url.Action("EditUser", "Account", new { id = user.Id })"
                                           class="btn btn-warning btn-sm" title=" ⁄œÌ·">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <button type="button" class="btn btn-danger btn-sm delete-btn" title=" ⁄ÿÌ· «·„” Œœ„"
                                                onclick="showToggleConfirmation(@user.Id, '@user.Name', true)">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr id="noDataRow">
                            <td colspan="7" class="no-data">·« ÌÊÃœ „” Œœ„Ê‰ ‰‘ÿÊ‰ ·⁄—÷Â„</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- Toggle Status Confirmation Modal -->
    <div class="modal fade custom-modal" id="toggleConfirmationModal" tabindex="-1" aria-labelledby="toggleConfirmationModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="toggleConfirmationModalLabel"> √ﬂÌœ  €ÌÌ— «·Õ«·…</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p id="toggleMessage"></p>
                    <p class="text-danger">·« Ì„ﬂ‰ «· —«Ã⁄ ⁄‰ Â–« «·≈Ã—«¡.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">≈·€«¡</button>
                    <button type="button" class="btn btn-danger" id="confirmToggleBtn"> √ﬂÌœ</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Form for Toggle Status -->
    <form id="toggleForm" asp-controller="Account" asp-action="ToggleUserStatus" method="post" style="display:none;">
        @Html.AntiForgeryToken()
        <input type="hidden" name="id" id="userIdToToggle" />
    </form>

    <div id="noSearchResults" class="no-results" style="display: none;">
                ·«  ÊÃœ ‰ «∆Ã  ÿ«»ﬁ «·»ÕÀ «·Œ«’ »ﬂ
            </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>

    <script>
        // Search functionality with enhanced username checking
        function performSearch() {
            const query = document.getElementById('userSearchInput').value.trim().toLowerCase();
            const rows = document.querySelectorAll('#usersTableBody tr[data-username]');
            let visibleCount = 0;
            let hasExactUsernameMatch = false;

            // If query is empty, show all rows
            if (query === '') {
                rows.forEach(row => {
                    row.style.display = '';
                    visibleCount++;
                });

                const noDataRow = document.getElementById('noDataRow');
                if (noDataRow) {
                    if (visibleCount === 0) {
                        noDataRow.style.display = '';
                        noDataRow.querySelector('td').textContent = '·« ÌÊÃœ „” Œœ„Ê‰ ‰‘ÿÊ‰ ·⁄—÷Â„';
                    } else {
                        noDataRow.style.display = 'none';
                    }
                }
                return;
            }

            // Check for exact username match first
            rows.forEach(row => {
                const username = row.getAttribute('data-username') || '';
                if (username === query) {
                    hasExactUsernameMatch = true;
                }
            });

            // Filter rows based on search query
            rows.forEach(row => {
                const username = row.getAttribute('data-username') || '';
                const name = row.getAttribute('data-name') || '';
                const role = row.getAttribute('data-role') || '';

                // Search in username, name, and role
                const matches = username.includes(query) ||
                               name.includes(query) ||
                               role.includes(query);

                if (matches) {
                    row.style.display = '';
                    visibleCount++;
                } else {
                    row.style.display = 'none';
                }
            });

            // Handle "no data" message
            const noDataRow = document.getElementById('noDataRow');
            if (noDataRow) {
                if (visibleCount === 0) {
                    noDataRow.style.display = '';

                    // Check if the search query looks like a username search (no spaces, specific format)
                    // and if there's no exact username match, show the specific message
                    if (!hasExactUsernameMatch && !query.includes(' ')) {
                        // Check if any username partially matches - if not, it's likely a username search
                        let hasPartialUsernameMatch = false;
                        rows.forEach(row => {
                            const username = row.getAttribute('data-username') || '';
                            if (username.includes(query)) {
                                hasPartialUsernameMatch = true;
                            }
                        });

                        if (!hasPartialUsernameMatch) {
                            noDataRow.querySelector('td').textContent = '·« ÌÊÃœ „” Œœ„ »Â–« «·«”„';
                        } else {
                            noDataRow.querySelector('td').textContent = '·« ÌÊÃœ „” Œœ„Ê‰ Ìÿ«»ﬁÊ‰ «·»ÕÀ «·Õ«·Ì';
                        }
                    } else {
                        noDataRow.querySelector('td').textContent = '·« ÌÊÃœ „” Œœ„Ê‰ Ìÿ«»ﬁÊ‰ «·»ÕÀ «·Õ«·Ì';
                    }
                } else {
                    noDataRow.style.display = 'none';
                }
            }
        }

        // Search button click event
        document.getElementById('userSearchBtn').addEventListener('click', performSearch);

        // Search on Enter key press
        document.getElementById('userSearchInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                performSearch();
            }
        });

        // Optional: Real-time search as user types (uncomment if you want this feature)
        /*
        document.getElementById('userSearchInput').addEventListener('input', performSearch);
        */
    </script>

    <script>
        // Global variable to store the ID of the user to toggle
        let userIdToToggle = 0;

        function showToggleConfirmation(id, name, isActive) {
            userIdToToggle = id;
            const action = isActive ? ' ⁄ÿÌ·' : ' ›⁄Ì·';
            const message = `Â· √‰  „ √ﬂœ „‰ ${action} «·„” Œœ„ <strong>${name}</strong>ø`;

            document.getElementById('toggleMessage').innerHTML = message;
            document.getElementById('userIdToToggle').value = id;

            var toggleModal = new bootstrap.Modal(document.getElementById('toggleConfirmationModal'));
            toggleModal.show();
        }

        document.getElementById('confirmToggleBtn').addEventListener('click', async function () {
            // Hide the modal
            var toggleModal = bootstrap.Modal.getInstance(document.getElementById('toggleConfirmationModal'));
            toggleModal.hide();

            const form = document.getElementById('toggleForm');

            try {
                const formData = new FormData(form);

                const response = await fetch('@Url.Action("ToggleUserStatus", "Account")', {
                    method: 'POST',
                    body: formData
                });

                if (response.ok) {
                    // ≈⁄«œ…  Õ„Ì· «·’›Õ… ·≈ŸÂ«— «· ÕœÌÀ« 
                    location.reload();
                } else {
                    alert('ÕœÀ Œÿ√ √À‰«¡  €ÌÌ— Õ«·… «·„” Œœ„. Ì—ÃÏ «·„Õ«Ê·… „—… √Œ—Ï.');
                }
            } catch (error) {
                alert('ÕœÀ Œÿ√ √À‰«¡ «·⁄„·Ì…. Ì—ÃÏ «· Õﬁﬁ „‰ « ’«· «·‘»ﬂ….');
                console.error(error);
            }
        });
    </script>

    <script>
        let allRows = [];
        let totalUsers = 0;

        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('searchInput');
            const clearButton = document.getElementById('clearSearch');
            const searchResults = document.getElementById('searchResults');
            const tableBody = document.getElementById('usersTableBody');
            const noSearchResults = document.getElementById('noSearchResults');

            // Get all user rows
            allRows = Array.from(document.querySelectorAll('#usersTableBody tr[data-username]'));
            totalUsers = allRows.length;

            // Update search results info on page load
            updateSearchResultsInfo('');

            // Search input event listener
            searchInput.addEventListener('input', function() {
                const searchTerm = this.value.trim();
                performSearch(searchTerm);

                // Show/hide clear button
                if (searchTerm) {
                    clearButton.style.display = 'block';
                } else {
                    clearButton.style.display = 'none';
                }
            });

            // Clear search button
            clearButton.addEventListener('click', function() {
                searchInput.value = '';
                performSearch('');
                this.style.display = 'none';
                searchInput.focus();
            });

            // Clear search on Escape key
            searchInput.addEventListener('keydown', function(e) {
                if (e.key === 'Escape') {
                    this.value = '';
                    performSearch('');
                    clearButton.style.display = 'none';
                }
            });
        });

        function performSearch(searchTerm) {
            const searchResults = document.getElementById('searchResults');
            const noSearchResults = document.getElementById('noSearchResults');

            if (!searchTerm) {
                // Show all rows
                allRows.forEach(row => {
                    row.classList.remove('hidden-row');
                    removeHighlights(row);
                });
                noSearchResults.style.display = 'none';
                updateSearchResultsInfo('');
                return;
            }

            const searchTermLower = searchTerm.toLowerCase();
            let visibleCount = 0;

            allRows.forEach(row => {
                const username = row.dataset.username || '';
                const name = row.dataset.name || '';
                const role = row.dataset.role || '';

                const isMatch = username.includes(searchTermLower) ||
                                name.includes(searchTermLower) ||
                                role.includes(searchTermLower);

                if (isMatch) {
                    row.classList.remove('hidden-row');
                    highlightSearchTerm(row, searchTerm);
                    visibleCount++;
                } else {
                    row.classList.add('hidden-row');
                    removeHighlights(row);
                }
            });

            // Show/hide no results message
            if (visibleCount === 0) {
                noSearchResults.style.display = 'block';
            } else {
                noSearchResults.style.display = 'none';
            }

            updateSearchResultsInfo(searchTerm, visibleCount);
        }

        function highlightSearchTerm(row, searchTerm) {
            // Highlight in the first three columns (name, username, role)
            const cells = [row.cells[0], row.cells[1], row.cells[2]];
            cells.forEach(cell => {
                if (!cell) return;
                const originalText = cell.dataset.originalText || cell.textContent;
                if (!cell.dataset.originalText) {
                    cell.dataset.originalText = originalText;
                }
                const regex = new RegExp(`(${escapeRegex(searchTerm)})`, 'gi');
                const highlightedText = originalText.replace(regex, '<span class="highlight">$1</span>');
                cell.innerHTML = highlightedText;
            });
        }

        function removeHighlights(row) {
            const cells = [row.cells[0], row.cells[1], row.cells[2]];
            cells.forEach(cell => {
                if (cell && cell.dataset.originalText) {
                    cell.textContent = cell.dataset.originalText;
                }
            });
        }

        function escapeRegex(string) {
            return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
        }

        function updateSearchResultsInfo(searchTerm, visibleCount = null) {
            const searchResults = document.getElementById('searchResults');

            if (!searchTerm) {
                if (totalUsers > 0) {
                    searchResults.innerHTML = `≈Ã„«·Ì ${totalUsers} „” Œœ„`;
                } else {
                    searchResults.innerHTML = '';
                }
            } else {
                const count = visibleCount !== null ? visibleCount : totalUsers;
                if (count === 0) {
                    searchResults.innerHTML = '·„ Ì „ «·⁄ÀÊ— ⁄·Ï ‰ «∆Ã';
                } else if (count === 1) {
                    searchResults.innerHTML = ' „ «·⁄ÀÊ— ⁄·Ï „” Œœ„ Ê«Õœ';
                } else if (count === 2) {
                    searchResults.innerHTML = ' „ «·⁄ÀÊ— ⁄·Ï „” Œœ„Ì‰';
                } else if (count <= 10) {
                    searchResults.innerHTML = ` „ «·⁄ÀÊ— ⁄·Ï ${count} „” Œœ„Ì‰`;
                } else {
                    searchResults.innerHTML = ` „ «·⁄ÀÊ— ⁄·Ï ${count} „” Œœ„`;
                }
            }
        }
    </script>
</body>
</html>